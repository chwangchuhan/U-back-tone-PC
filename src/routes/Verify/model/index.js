import { routerRedux } from 'dva';
import { login } from '../service';
import $$ from 'cmn-utils';

export default {
  namespace: 'verify',

  state: {
    loading: false,
  },

  subscriptions: {},

  effects: {
    
  },

  reducers: {

  }
};
